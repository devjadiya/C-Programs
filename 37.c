// Math functions
#include <stdio.h>
#include <math.h>
int main()
{
    // absolute function abs
    int x = abs(-150); // Syntax --> int a = abs(value of a);
    printf("The absolute value of x is : %d\n", x);
    return 0;
}